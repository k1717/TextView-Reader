// Top-level build file for TextView.
// Uses the modern Gradle plugins block instead of the old buildscript/allprojects style.
plugins {
    id 'com.android.application' version '8.13.1' apply false
}
